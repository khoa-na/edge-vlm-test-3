# Lõi — bắt buộc (demo + test chạy với mock backend)
numpy
pytest

# Tùy chọn — chạy thật Tier 1 với webcam
# mediapipe
# opencv-python

# Tùy chọn — phone detection thật (YOLO26n, kéo theo torch CPU)
# ultralytics

# Tùy chọn — cảnh báo TTS tiếng Việt offline
# piper-tts        # pre-render (build-time)
# sounddevice      # phát WAV (runtime; cần libportaudio2 của hệ thống)

# Tùy chọn — chạy thật Tier 2 với model GGUF
# llama-cpp-python
